import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Cambiarclave2Page } from './cambiarclave2.page';

describe('Cambiarclave2Page', () => {
  let component: Cambiarclave2Page;
  let fixture: ComponentFixture<Cambiarclave2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Cambiarclave2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
